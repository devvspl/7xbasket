<IfModule mod_rewrite.c>
    RewriteEngine On

    # Strip /public prefix if someone hits it directly (301 redirect)
    RewriteCond %{REQUEST_URI} ^/public/(.*)$
    RewriteRule ^ /%1 [L,R=301]
</IfModule>
